# Context.md — nh-deck

Living state-of-the-world doc. Agents should update this as work progresses — this is not a duplicate of `AGENTS.md`'s static command list.

## What this is

nh-deck is a local-first CLI for writing, presenting, and exporting Markdown-based slide decks. It is one of three independent sibling projects — daily-dose, nh-deck, nh-skills — under the **Not-Humans-Lab** umbrella (`../Not-Humans-Lab/`, a separate docs-only meta-repo). nh-deck is itself a standalone GitHub repository, matching this workspace's polyrepo convention — it is not nested inside Not-Humans-Lab.

**Corrected assumption, for the record:** earlier speculative research about nh-deck guessed it might be a hosted multi-user product with "shareable decks" requiring auth. That assumption was wrong and is now superseded. nh-deck is confirmed local-first: no user accounts, no hosting, no multi-user sharing infrastructure. All docs in this repo, especially `SOUL.md` and the eventual `SECURITY.md`, are written to reflect this reality — not the discarded speculative one. If you find older reasoning anywhere that assumes hosted/multi-user, it is stale and should be corrected on sight.

nh-skills (`../nh-skills/`) and daily-dose (`../daily-dose/`) have both also shipped their own phases — nh-skills now has two skills through the loop, daily-dose ships both HN and arXiv sourcing — see their own repos/Context.md files for their state; their content is useful as house-style precedent but never copied verbatim, since each project's product shape is unrelated to nh-deck's (a rendering CLI).

## Current state (as of 2026-09-15, v1.6.0)

- **The original 12-item Roadmap (below) fully shipped as of v1.0.0** — render → serve → PDF/PNG export, KaTeX, Mermaid, named themes, per-slide layouts, transitions, and presentation mode all landed and are covered in detail in `CHANGELOG.md`, which is the authoritative record for everything since. This file's Roadmap section is kept below as a closed historical record; do not add new items to it — track new work in `CHANGELOG.md` and this section instead.
- **Seven backlog-prioritization releases have shipped since v1.0.0**, each a research-informed pass fixing real gaps and adding real features, not just polish:
  - **v1.1.0** — WCAG-contrast fixes, a presentation-mode progress bar, `prefers-reduced-motion` support, Home/End/Escape navigation, a grid-overview mode, touch swipe, colorized CLI output.
  - **v1.2.0** — fixed a real, shipped-broken bug (PDF/PNG export was portrait A4, not landscape 16:9); added fragment (incremental reveal) markers, a keyboard-shortcuts help overlay, `--css-vars` layered theme overrides.
  - **v1.3.0** — `--with-notes` export, `nh-deck init` (starter-deck scaffold), a genuine second-screen presenter-view window synced via `BroadcastChannel`.
  - **v1.4.0** — `nh-deck list-themes` + a theme-gallery README section, jump-to-slide (`g` + digits + Enter), watch-mode stdout feedback, post-render `?present`/`?notes` discoverability hints, an `ACCESSIBILITY.md` status doc.
  - **v1.4.1** — a full end-to-end production dry run (real CLI usage, real decks, real browser interaction, real exported files — not just code review) found and fixed one critical bug: fragment markers written as a trailing comment on the SAME line as bullet/paragraph text (the exact syntax the docs describe) were silently dropped. Also fixed a help-overlay discoverability gap and corrected two changelog entries that had overstated PNG export dimension guarantees beyond what `docs/adr/0006-phase-5-polish.md` actually documents.
  - **v1.5.0** — a small fast-follow closing out the last item deliberately deferred from the presenter-view window (v1.3.0): the presenter-view timer can now be paused/resumed (a real, keyboard-operable button), plus an optional target duration that recolors the display amber/red as it approaches/exceeds that time, persisted to `localStorage` only (no CLI flag or frontmatter key).
  - **v1.6.0** — three items from a deep-research pass on the deferred backlog, all shipped with zero new dependencies: real PDF bookmark/outline navigation derived from the deck's own heading structure; a live theme-preview control during `render --watch` that reuses the existing SSE reload channel (including correct Mermaid recoloring); and opt-in RTL text direction via `--dir <name>`/`dir:` frontmatter, scoped to text direction only.
- **Tech stack: unchanged in kind, expanded in scope.** Still TypeScript on Node.js (22+ — Node 20 support dropped 2026-09-15, see Open risks below), Commander.js, `marked`, a plain `node:http` dev server, `puppeteer-core` + `chrome-launcher`, plain `tsc` (no bundler). Added since the walking skeleton: `katex`/`marked-katex-extension`, `beautiful-mermaid`, `svgo`, `open`, `pngjs` (dev-only, PNG dimension assertions). Tests via Vitest — 21 files, 400+ tests, including real unmocked browser/PDF/PNG export coverage.
- **CI: full cross-platform matrix, live and still green.** `ubuntu-latest` / `macos-14` / `windows-latest` × Node 22/latest (6 combinations, down from 9 now that Node 20 is dropped — see Open risks), plus a dedicated cross-browser PDF/PNG fidelity job and GitGuardian secret scanning.
- **License: Apache-2.0**, decided once at the Not-Humans-Lab system level and applied identically across all three sibling projects (see `../Not-Humans-Lab/decisions.md`).
- **npm publish: blocked, not yet shipped.** The package has never successfully published (confirmed 404 on the registry as of 2026-09-14) — blocked by a 72-hour npm security hold on this account, tracked separately with a reminder for around 2026-09-16. This is the single largest gap between "fully built and dry-run-verified" and "actually installable by a real user" — treat it as the next real milestone, not a backlog item.

## Architecture at a glance

Three layers, all local, none of them a service:

1. **Render** — `marked`-based Markdown-to-HTML conversion (`src/render.ts`, exporting `generateHtml`). Deck source is a single Markdown file; output is static HTML. No CDN dependency for correctness — anything the rendered HTML needs must ship locally.
2. **Serve** — a plain `node:http` dev server (`src/server.ts`, exporting `startServer`) that serves the rendered deck locally, for presenting and for live iteration while writing. No framework (no Express), matching the "no bundler, no unnecessary dependency" philosophy running through this project's tooling choices.
3. **Export** — PDF export (`src/pdfExport.ts`, exporting `exportToPdf`) via `puppeteer-core` driving a locally-detected Chrome/Chromium/Edge/Brave binary through `chrome-launcher`. No bundled browser binary ships with nh-deck or gets downloaded at install time.

The CLI surface (`src/index.ts`, via Commander.js) wires the `render` and `pdf` subcommands to these three modules directly — one flat file per concern, no subdirectory nesting at this project's current size. See `AGENTS.md`'s Directory Map for the concrete file layout, and `../Not-Humans-Lab/architecture.md` for how nh-deck fits into the three-project system (C4 Level 1 only — this repo owns its own internals).

## Key decisions & why

- **Local-first, no exceptions, in the core path** — the entire reason to build this tool instead of using a hosted deck tool is to never depend on a network connection or a third party's server for something as basic as showing your own slides. See `SOUL.md` for the full non-negotiable and `CLAUDE.md` for the stop-and-ask gate that protects it during agent-assisted changes.
- **KaTeX and Mermaid both shipped CDN-free, not stubbed or CDN-shimmed** — both were real, wanted features whose local, bundled-asset implementation was more work than any single walking-skeleton phase allowed on its own, so each landed as its own dedicated phase (see Current state above) rather than being folded silently into an unrelated change or shortcut via a CDN reference.
- **No bundler (`tsc` only)** — keeps the build step legible for a CLI this small; revisit only if a real, demonstrated need (not a hypothetical optimization) shows up.
- **`puppeteer-core` + `chrome-launcher` over full `puppeteer` or Playwright** — avoids bundling or downloading a Chromium binary, which would be both a larger install footprint and a silent network dependency at `npm install` time — a direct conflict with the local-first constraint.
- **Single-OS CI first, then the full matrix once green** — the full 3-OS matrix was real work (especially for `chrome-launcher`'s OS-specific binary detection paths), sequenced after the skeleton went green rather than paying that cost upfront. It has since shipped (see Current state above).
- **License = Apache-2.0** — decided once at the umbrella level, not re-decided per project; see `../Not-Humans-Lab/decisions.md` for the patent-grant rationale.

## Roadmap

In order — do not build out of sequence. Reconciled 2026-09-10 against a full codebase index (`CODEBASE_INDEX.md`) and three primary-source research docs (`docs/research/*.md`); full rationale in `docs/specs/feature-implementation-roadmap-design.md`.

1. ~~Finish the Phase 4 walking skeleton.~~ Done — render → serve → PDF-export core loop verified end-to-end.
2. ~~Expand CI to the full 3-OS × multi-Node-version matrix.~~ Done — 9/9 combinations green, including the real PDF-export test on every OS.
3. ~~Quick wins: 5 code-robustness fixes (render try/catch, --port validation, pdf output-path derivation, puppeteer launch/finally) + CI/tooling hardening (npm cache, npm audit, Biome + typecheck gate, PR-checklist doc-drift item).~~ Done — merged to main.
4. ~~Real per-slide segmentation (`---` → `<section>` boundaries).~~ Done — `generateHtml()` now splits on `---` via `marked`'s lexer/parser token-group split (see `docs/adr/0002-per-slide-segmentation.md`). Foundational for live-reload (item 5) and presenter notes/PDF pagination (item 6).
5. ~~Genuine live-reload for `render`.~~ Done — `render --watch` now re-renders and pushes a same-origin SSE reload event on file change (see `docs/adr/0003-sse-based-live-reload.md`); the previously-false "live reload" claim in `AGENTS.md`'s Overview is now accurate.
6. ~~Presenter notes (HTML-comment convention) + per-slide PDF pagination — both depend on item 4.~~ Done (see `docs/adr/0006-phase-5-polish.md`).
7. ~~Automated cross-browser PDF-export visual-fidelity check (unchanged from the prior roadmap's item 3 fast-follow — independent of items 3–6 and 8–11 below).~~ Done — `scripts/check-pdf-fidelity.mjs` (`npm run check:fidelity`) compares PNG pixel-diff (2% tolerance) and PDF page counts across the first two `chrome-launcher`-detected browsers, gracefully skipping below 2, enforced in CI via a dedicated `pdf-fidelity-check` job; see `docs/adr/0007-pdf-cross-browser-fidelity-check.md`.
8. ~~KaTeX (math rendering).~~ Done — inline and block LaTeX math now renders via KaTeX with fonts embedded locally as base64 (no CDN fallback path); see `docs/adr/0004-katex-local-embedded-math.md`.
9. ~~Mermaid (diagram rendering), local-only.~~ Done — `mermaid` fenced code blocks now render as embedded SVG diagrams with the library's own CDN font import stripped (no CDN fallback path); see `docs/adr/0005-mermaid-local-cdn-import-stripped.md`.
10. ~~`--css <path>` opt-out flag for the baseline stylesheet — a smaller, immediately-actionable slice of item 11.~~ Done (see `docs/adr/0006-phase-5-polish.md`).
11. ~~Named theme system (the theme portion of "Full theme, template, and transition system").~~ Done — 4 fixed, opt-in named color themes (`light`, `dark`, `dracula`, `nord`), selectable via `--theme <name>` or a deck's own frontmatter `theme:` key, applied consistently to the base deck and KaTeX math; `--css` still wins over any theme, and a deck with no theme requested renders byte-identical to pre-feature output (see `docs/adr/0008-named-theme-system.md`). ~~**Mermaid diagrams are not yet actually recolored by the active theme.**~~ Fixed (see `docs/adr/0008-named-theme-system.md`'s Consequences, amended) — `render.ts` threads the active theme's colors into the Mermaid `code()` renderer branch via a module-level "current call" variable; see the regression test `recolors Mermaid diagrams to match the active theme` in `tests/render.test.ts`. ~~**Templates and transitions remain deferred.**~~ Done — 4 fixed, opt-in per-slide layouts (`title`, `section`, `two-column`, `quote`), selectable via a per-slide `<!-- layout: name -->` marker comment and applied everywhere (`render`, `pdf`, `png`); deck-wide `fade`/`slide` transitions, selectable via `--transition <name>` or a deck's own frontmatter `transition:` key (flag wins), applying only inside the new opt-in one-slide-at-a-time presentation mode (`?present` in the URL, mirroring the existing `?notes` toggle). `--css` still wins over both layout and transition CSS, and a deck with no layout marker/no `--transition`/frontmatter `transition:`/no `?present` renders and behaves byte-identically to pre-feature output. Presentation mode itself was a genuine mid-brainstorm scope discovery, not originally planned — transitions are meaningless without discrete slide changes to animate between, and no slide-by-slide navigation mechanism existed yet. See `docs/adr/0009-templates-transitions-presentation-mode.md` for the full record.
12. ~~PNG (and, further out, PPTX) export via the already-detected browser — depends on item 4.~~ Done — PNG export implemented (see `docs/adr/0006-phase-5-polish.md`); PPTX explicitly remains out of scope.

**This roadmap is fully shipped and closed as of v1.0.0.** Everything since (v1.1.0 through v1.6.0, see Current state above) came from separate, research-informed backlog-prioritization passes rather than this numbered sequence — do not append new items here. `CHANGELOG.md` is the ongoing source of truth for what shipped and when; this section stays as the historical record of the original 12-item plan.

## Open risks

- ~~**Mermaid diagrams are not yet actually recolored by the named theme system (Roadmap item 11).**~~ **Resolved** — `generateHtml`'s Mermaid `code()` renderer branch now receives the active theme's colors; see `tests/render.test.ts`'s `recolors Mermaid diagrams to match the active theme` regression test and `docs/adr/0008-named-theme-system.md`'s amended Consequences.
- **The local-first constraint has now been tested against real "convenience" pressure twice, and held both times.** KaTeX's own upstream convention (and two of the five tools surveyed during roadmap research, reveal.js's and Marp Core's official plugins) defaults to a CDN font path — this project instead ships KaTeX's fonts embedded as base64 `data:` URIs directly in rendered output, with no CDN fallback path at all, verified via a real end-to-end browser check (zero network requests beyond the local file). Separately, Mermaid's chosen renderer library (`beautiful-mermaid`) turned out to bake a hardcoded Google Fonts CDN `@import` into its own default output — a real proposed CDN dependency, not a hypothetical one, discovered by re-checking rendered output content rather than just the npm dependency tree. It was found and stripped rather than shipped. See `docs/adr/0004-katex-local-embedded-math.md` and `docs/adr/0005-mermaid-local-cdn-import-stripped.md`.
- ~~**No cross-platform PDF export *fidelity* check exists yet.**~~ **Resolved 2026-09-11** — see Roadmap item 7 and `docs/adr/0007-pdf-cross-browser-fidelity-check.md`; a dedicated CI job now compares pixel and page-count output across two distinct Chrome-family browsers, gracefully skipping on any machine with fewer than 2 detected.
- **The PDF-export test is genuinely slow under machine contention** — locally observed 5-55s depending on concurrent load; CI runners are dedicated so this shouldn't recur there, but the test's 60s timeout is worth revisiting if it ever proves too tight or too loose in practice.
- ~~**`vitest`/`vite`/`esbuild` dev-only dependency chain has known moderate/critical advisories.**~~ **Resolved 2026-09-11** — bumped `vitest` `^2.1.3` → `^5.0.0` (latest v5.x on npm at the time), which pulled in a patched `vite`/`esbuild`/`@vitest/mocker` chain. Also required bumping `@types/node` `^20.14.0` → `^22.10.0` to satisfy `vitest@5.0.0`'s optional peer dependency (`^22.0.0 || >=24.0.0`) — a direct, unavoidable consequence of the major bump, not a scope-unrelated change; plain `npm install` (no `--legacy-peer-deps`/`--force`) now resolves cleanly. Full, unscoped `npm audit` went from 5 vulnerabilities (1 critical, 1 high, 3 moderate) to 0. Full test suite (120/120 across 15 files, after this branch's other additions) passes unchanged against vitest 5 — no config/API breakage encountered. Zero production exposure either way (dev/test-tooling only, never shipped in `dist/`).
- ~~**`puppeteer-core@25.10.0` and its transitive `@puppeteer/browsers@3.2.2` declare `"engines": {"node": ">=22.12.0"}`, stricter than this project's own Node 20 support commitment.**~~ **Superseded 2026-09-15 — Node 20 support dropped, but not for this reason.** The original investigation's ruling (keep Node 20; the `engines.node` mismatch was verified warning-only and non-breaking on real Node 20.20.2, full test suite included) still holds on its own terms and is preserved here for the record. What actually ended Node 20 support was an independent, later event: Node 20 reached its own upstream end-of-life on 2026-04-30. That is a real, vendor-driven functional-support decision, unlike the engine-mismatch warning this entry originally tracked — so it was not the "reflexive fix" the original ruling warned against. `package.json`'s `engines.node` is now `>=22`, `AGENTS.md`'s Setup section and this Known Gotchas cross-reference were updated to match, and the CI matrix dropped Node 20 (now `[22, latest]`, 6 combinations instead of 9 — see Current state above).

---
*Last updated: 2026-09-15 (v1.6.0). Agents: keep this current as work progresses — do not let it go stale while `AGENTS.md`/`SOUL.md`/`CLAUDE.md` stay static.*
